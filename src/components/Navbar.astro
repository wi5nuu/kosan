<nav id="navbar" class="navbar">
  <div class="container nav-inner">
    <a href="#" class="logo">
      <img src="public/logo.png" alt="Al'ashar Kos Logo" class="logo-img">
    </a>

    <div class="nav-links">
      <a href="#tentang">Tentang</a>
      <a href="#fasilitas">Fasilitas</a>
      <a href="#galeri">Galeri</a>
      <a href="#paket">Paket</a>
      <a href="#lokasi">Lokasi</a>
    </div>

    <div class="nav-actions">
      <a href="https://wa.me/08139488240" target="_blank" class="nav-btn-cta">
        <span>Booking Now</span>
      </a>
      
      <button class="hamburger" id="hamburger" aria-label="Menu">
        <div class="bar"></div>
        <div class="bar"></div>
      </button>
    </div>
  </div>

  <div class="mobile-overlay" id="mobileMenu">
    <div class="mobile-nav-content">
      <a href="#tentang" class="mob-link">Tentang</a>
      <a href="#fasilitas" class="mob-link">Fasilitas</a>
      <a href="#galeri" class="mob-link">Galeri</a>
      <a href="#paket" class="mob-link">Paket</a>
      <a href="#lokasi" class="mob-link">Lokasi</a>
      <a href="https://wa.me/08139488240" class="mob-cta">Chat WhatsApp</a>
    </div>
  </div>
</nav>

<style>
:root {
  --nav-height: 85px;
  --nav-bg: transparent;
  --accent: #e67e22;
  --nav-text-light: #ffffff;
  --nav-text-dark: #1a1a1a;
}

.navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: var(--nav-height);
  z-index: 2000;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
}

/* Navbar Scrolled State */
.navbar.scrolled {
  height: 70px;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(15px);
  -webkit-backdrop-filter: blur(15px);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
}

.nav-inner {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* Logo Styling - Ukuran Kecil & Proporsional */
.logo {
  display: flex;
  align-items: center;
  gap: 10px;
  text-decoration: none;
}

.logo-img {
  height: 32px; /* Ukuran logo diperkecil agar elegan */
  width: auto;
  object-fit: contain;
  transition: transform 0.3s ease;
}

.navbar.scrolled .logo-img {
  height: 28px; /* Lebih kecil sedikit saat scroll */
}

.logo-text {
  font-size: 1.05rem;
  font-weight: 500;
  color: var(--nav-text-light);
  letter-spacing: -0.3px;
  transition: color 0.3s;
}

.navbar.scrolled .logo-text {
  color: var(--nav-text-dark);
}

.logo-text strong {
  color: var(--accent);
  font-weight: 800;
}

/* Desktop Menu */
.nav-links {
  display: flex;
  gap: 30px;
}

.nav-links a {
  color: var(--nav-text-light);
  text-decoration: none;
  font-size: 0.9rem;
  font-weight: 500;
  transition: all 0.3s;
  opacity: 0.85;
}

.navbar.scrolled .nav-links a {
  color: var(--nav-text-dark);
  opacity: 1;
}

.nav-links a:hover {
  color: var(--accent) !important;
  opacity: 1;
}

/* CTA Button Modern */
.nav-btn-cta {
  background: var(--accent);
  color: white;
  padding: 10px 22px;
  border-radius: 12px;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.85rem;
  transition: all 0.3s;
  box-shadow: 0 4px 15px rgba(230, 126, 34, 0.2);
}

.nav-btn-cta:hover {
  background: #d35400;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(230, 126, 34, 0.3);
}

/* Hamburger & Mobile Menu tetap sama namun warna bar adaptif */
.hamburger {
  display: none;
  flex-direction: column;
  gap: 6px;
  background: none;
  border: none;
  cursor: pointer;
  z-index: 2100;
}

.bar {
  width: 24px;
  height: 2px;
  background: white;
  transition: 0.3s;
}

.navbar.scrolled .bar { background: var(--nav-text-dark); }

@media (max-width: 850px) {
  .nav-links, .nav-btn-cta { display: none; }
  .hamburger { display: flex; }
}

/* Mobile Overlay Tetap */
.mobile-overlay {
  position: fixed;
  top: 0;
  right: -100%;
  width: 100%;
  height: 100vh;
  background: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: 0.5s cubic-bezier(0.77, 0.2, 0.05, 1);
  z-index: 2050;
}
.mobile-overlay.open { right: 0; }
.mobile-nav-content { display: flex; flex-direction: column; align-items: center; gap: 25px; }
.mob-link { font-size: 1.4rem; font-weight: 700; color: #1a1a1a; text-decoration: none; }
.mob-cta { background: var(--accent); color: white; padding: 14px 35px; border-radius: 12px; text-decoration: none; font-weight: 600; }
</style>

<script>
  const navbar = document.getElementById('navbar');
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobileMenu');

  // Efek Scroll yang lebih smooth
  window.addEventListener('scroll', () => {
    if (window.scrollY > 30) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
  });

  // Toggle Mobile Menu
  hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    mobileMenu.classList.toggle('open');
    document.body.style.overflow = mobileMenu.classList.contains('open') ? 'hidden' : 'auto';
  });

  // Close menu saat link diklik
  document.querySelectorAll('.mob-link').forEach(link => {
    link.addEventListener('click', () => {
      hamburger.classList.remove('active');
      mobileMenu.classList.remove('open');
      document.body.style.overflow = 'auto';
    });
  });
</script>